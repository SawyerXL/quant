# 已否决策略 — 归档脚本一览

本目录存放经回测验证**未通过、已否决**的策略脚本，仅留档，不再运行（cron 已移除）。

| 脚本 | 策略 | 否决原因 |
|------|------|---------|
| `backtest_csi500_*.py` | CSI500 N=6 中盘 | OOS +39.6% 是日期错位 bug 假象，真实 +2.7%/-73% 回撤（详见 README_CSI500_BUG.md） |
| `top3_basket.py` | Top3 集中篮子 | CSI800 上 3 种构造方法（v1/v2/v3）OOS 验收全部不达标 |
| `daily_signal_b.py` | Track B 三位一体 | 强势股方法论个股层无稳定 alpha，已否决（trinity 模块见 strategies/archive） |

**唯一在跑的实盘策略 = Track A**（多因子月度/双周调仓，`daily_signal_a.py` + `run_backtest_a.py`），
其回测引擎用全日级预分配索引，不受上述日期错位 bug 影响，验证指标成立。

教训：任何新策略上实盘前，必须过同一套严苛验证 —— 历史成分股池(防生存者偏差) + 按市价正确记账 + OOS + 双边成本，且净值日期标签 = 真实记录日期。
